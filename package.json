{
  "name": "d1-template",
  "version": "1.0.0",
  "description": "DarkChat Cloudflare Worker",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute darkchat-db --file=./migrations/0001_initial.sql",
    "db:remote": "wrangler d1 execute darkchat-db --command=\"SELECT name FROM sqlite_master WHERE type='table';\""
  },
  "keywords": ["chat", "cloudflare", "worker", "d1"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.30.0"
  }
}